import Counter from "@/components/Counter/Counter";

const Home = () => {

  // throw new Error()
  return (
    <div>
      <h1>This is from next js app</h1>
      <Counter></Counter>
    </div>
  );
};

export default Home;